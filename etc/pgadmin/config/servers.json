{
  "Servers": {
      "1": {
          "Name": "postgres@postgres",
          "Group": "Servers",
          "Host": "postgres",
          "Port": 5432,
          "MaintenanceDB": "postgres",
          "Username": "postgres",
          "PassFile": "/etc/pgpass",
          "SavePassword": true,
          "SSLMode": "prefer",
          "ConnectionParameters": {
            "sslmode": "prefer",
            "connect_timeout": 10,
            "passfile": "/etc/pgpass"
        }
      }
  }
}